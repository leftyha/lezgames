import { getGame, games } from '@lezgamez/catalog';import { notFound } from 'next/navigation';
export function generateStaticParams(){return games.map(g=>({slug:g.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params; const g=getGame(slug); return {title:`Play shell — ${g?.title}`,alternates:{canonical:`/games/${slug}`}}}
export default async function Play({params}:{params:Promise<{slug:string}>}){const {slug}=await params; const g=getGame(slug); if(!g) notFound(); return <main><h1>Game Shell: {g.title}</h1><p className="muted">Pre-play gate. Assets are not loaded until Play is pressed. Adblock check blocks play with the official message.</p><div className="stage shell"><div><h2>READY // LAUNCH SESSION MOCK</h2><p className="muted">Adblock detected. LezGamez is free because ads keep the games online. Please disable your adblocker to play.</p><p><button className="btn">Play</button> <a className="btn dark" href="/games">Exit to catalog</a></p></div></div></main>}
